import MainContent from "./MainContent"
import MainSidebar from "./MainSidebar"

const Main = () => {
  return (
    <main className="flex">
        <MainContent />
        <MainSidebar />
    </main>
  )
}

export default Main